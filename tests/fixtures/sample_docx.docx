This is a mock DOCX file. It is currently a plain text file with a .docx extension.
In a real environment, this should be replaced with a valid OpenXML document.
It is intended for testing the ingestion pipeline's ability to route .docx files to the correct extractor.
